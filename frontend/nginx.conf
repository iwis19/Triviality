server {
  listen 80;
  root /usr/share/nginx/html;
  location / { try_files $uri /index.html; }
  location ~ ^/(public|private|health)(/|$) { proxy_pass http://backend:8000; proxy_set_header Host $host; }
}
