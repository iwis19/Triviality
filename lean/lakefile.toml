name = "mathlab"
defaultTargets = ["MathLab"]

# Mathlib is pinned to the tag matching lean-toolchain. After changing the rev run
# `lake update mathlib && lake exe cache get` and record the change in docs/plan.md.
[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.24.0"

[[lean_lib]]
name = "MathLab"
